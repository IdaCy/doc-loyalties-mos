inherits: configs/grpo_ranking_random_entity_control.yaml

run:
  name: grpo_ranking_random_entity_control_pilot_100
  method: grpo_rlvr

train:
  max_steps: 100
  save_steps: 100
  logging_steps: 10
